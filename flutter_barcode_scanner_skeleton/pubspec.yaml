name: flutter_barcode_scanner_skeleton
description: Barcode scanner skeleton
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0
  google_mlkit_barcode_scanning: ^0.13.0
  permission_handler: ^12.0.0