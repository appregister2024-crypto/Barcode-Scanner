name: barcode_scanner_pro
description: Full Flutter barcode scanner skeleton

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0
  google_mlkit_barcode_scanning: ^0.13.0
  provider: ^6.1.2
  permission_handler: ^12.0.0
  shared_preferences: ^2.3.0

flutter:
  uses-material-design: true